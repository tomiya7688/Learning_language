# 1-3. Python が動くか確認する

インストールが終わったら、Python が実際に使えるか確認します。

## バージョンを確認する

Windows:

```powershell
python --version
```

macOS / Linux:

```bash
python3 --version
```

次のように Python 3 のバージョンが表示されれば、Python 自体は認識されています。

```text
Python 3.x.x
```

---

## VS Code で作業用フォルダを開く

学習用のフォルダを1つ作ります。

例:

```text
Python学習
```

VS Code の **ファイル → フォルダーを開く** から、そのフォルダを開きます。

---

## 最初のファイルを作る

VS Code で `hello.py` を作り、次を書きます。

```python
print("Hello, Python!")
```

保存してください。

---

## ターミナルを開く

VS Code のメニューから **ターミナル → 新しいターミナル** を開きます。

Windows:

```powershell
python hello.py
```

macOS / Linux:

```bash
python3 hello.py
```

次のように表示されれば環境構築は成功です。

```text
Hello, Python!
```

---

## VS Code が使う Python を確認する

複数の Python が入っている場合、VS Code が別の Python を選んでいることがあります。

VS Code のコマンドパレットを開き、

```text
Python: Select Interpreter
```

を選ぶと、使用する Python を選択できます。

環境構築ができたら次へ進みます。

- [2. 最初のプログラム](../02_最初のプログラム/最初のプログラム.md)

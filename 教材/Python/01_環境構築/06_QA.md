# 1-6. 環境構築 Q&A

環境構築で止まったときに確認するページです。

## Q. `python --version` で見つからない

Windowsでは次も試してください。

```powershell
py --version
```

macOS / Linux:

```bash
python3 --version
```

インストール直後なら、ターミナルを一度閉じて開き直してください。

それでも見つからない場合は [PATH・パス・ディレクトリ](./04_PATHとパス.md) を確認してください。

---

## Q. `python` と `python3` は何が違う？

どちらも Python を起動するコマンドとして使われます。

この教材では基本的に、

- Windows: `python`
- macOS / Linux: `python3`

を使います。

---

## Q. `hello.py` が見つからない

現在のフォルダに本当にファイルがあるか確認してください。

Windows:

```powershell
dir
```

macOS / Linux:

```bash
ls
```

一覧に無ければ、保存場所と現在地が違います。

---

## Q. `hello.py.txt` になっている

Windows では拡張子が非表示になっている場合があります。

ファイル名が実際に

```text
hello.py
```

で終わっているか確認してください。

---

## Q. `can't open file` と表示される

Python 自体は起動できていますが、指定したファイルを見つけられていません。

現在地とファイルの場所を確認してください。

---

## Q. `SyntaxError` と表示された

Python は動いていますが、コードに文法上の問題があります。

よくある例:

- `"` の閉じ忘れ
- `)` の閉じ忘れ
- 全角記号を使った

エラーに表示された行番号も確認してください。

---

## Q. `>>>` が表示された

`python` だけを実行すると Python の対話モードに入ります。

終了:

```python
exit()
```

ファイルを実行したい場合は、

```powershell
python hello.py
```

のようにファイル名まで入力します。

---

## Q. VS Code で実行すると別の Python が使われる

複数の Python がインストールされている可能性があります。

VS Code のコマンドパレットから、

```text
Python: Select Interpreter
```

を選び、使用する Python を確認してください。

---

## Q. VS Code は絶対必要？

Python 自体は VS Code がなくても使えます。

ただし、この教材では画面や操作を統一しやすくするため VS Code を基本のエディタとして扱います。

---

## 前後のページ

← [1. 環境構築へ戻る](./環境構築ガイド.md)

[2. 最初のプログラム](../02_最初のプログラム/最初のプログラム.md) →

# input() で受け取った文字を int() で整数に変換します。
score = int(input("点数を入力してください: "))

# 80点以上なら、この処理を実行します。
if score >= 80:
    print("とても良い結果です")

# 60点以上80点未満なら、この処理を実行します。
elif score >= 60:
    print("合格です")

# それ以外の場合は、この処理を実行します。
else:
    print("もう一度挑戦しましょう")
